# ps-bitwarden
A powershell module that allows creation of Bitwarden vault items and sends using powershell instead of the gui.

## Usage
Import the module into your PowerShell and run Get-Command -Module "matmcp1-ps-bitwarden". Run one of these commands along with desired params.